window.onload = function() {
    var config = {
        width: 800,
        height: 600,
        backgroundColor: 0x000000,
        scene: [ Scene00, Scene01 ]
    }

    var game = new Phaser.Game(config);
}